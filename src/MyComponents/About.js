import React from 'react'

export const About = () => {
    return (
        <div>

This is web app named ToDo list contains login/signup features along with create,delete,and update features .To run the source code you first need to install node i.e "npm install node" and write the command "npm starat "
        </div>
    )
}
